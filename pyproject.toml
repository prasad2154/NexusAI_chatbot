[project]
name = "nexusai-chatbot"
version = "0.1.0"
description = "NexusAI Chatbot Application"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "httpx>=0.27.0",
    "requests>=2.32.0",
    "streamlit>=1.35.0",
    "uvicorn>=0.30.0"
]

[tool.vercel]
entrypoint = "backend/app.py"
